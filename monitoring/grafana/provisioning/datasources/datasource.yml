# Auto-registers Prometheus as a Grafana data source on container start,
# so there's no manual "Add data source" click-through for a fresh setup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
